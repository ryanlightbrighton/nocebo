// animal config

class Animal : Man {
	// class EventHandlers {
	// 	class Nocebo {
	// 		local = "if (isServer and {alive (_this select 0)}) then {deleteVehicle (_this select 0)}"
	// 	};
	// };
	class InteractionInfo : InteractionInfo {
		class gutAnimal : gutAnimal {};
	};
};
class Animal_Base_F : Animal {
	horde_ambientEmitter = 1;
	horde_particles[] = {"Horde_Flies"};
	horde_sound[] = {{"horde_sound_flies",13}};
};